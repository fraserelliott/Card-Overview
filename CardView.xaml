<UserControl x:Class="card_overview_wpf.CardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:card_overview_wpf"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="100" >
    <Grid Background="Transparent">
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Select Image" Click="MenuItem_Click" />
                <MenuItem Header="Toggle Visible" Click="MenuItem_Click_1" />
            </ContextMenu>
        </Grid.ContextMenu>
        <Image x:Name="imageCard" Height="100" Width="100" Stretch="Fill" />
        <TextBox x:Name="textBoxCount" Margin="65,65,0,0" Text="0" BorderThickness="2,1,1,1" TextWrapping="Wrap" IsReadOnly="True" TextAlignment="Center" FontSize="21.333"/>
    </Grid>
</UserControl>
